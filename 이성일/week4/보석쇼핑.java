class Solution {
    public int[] solution(String[] gems) {
        int[] answer = {};
        class Solution {
            public int[] solution(String[] gems) {
                int[] answer = {};
                // 서로 다른 원소의 개수 구하기
                // 집합을 이용하기
                // 첫번째 원소부터 슬라이딩
                // 다음원소가 현재 슬라이드에 들어갈 수 있는 지 확인하기
                // 있다면 기존 요소에 추가하기
                // 이미 슬라이드에 있다면
                // 현재 원소 이전 원소부터 슬라이드 처음까지 현재원소와 같은 원소를 만나기 전까지
                // 기존 슬라이드 원소개수와 같아지는 시점이 있다면
                // 없다면 슬라이드 연장
                return answer;
            }
        }
        return answer;
    }
}
